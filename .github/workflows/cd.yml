name: CD - Deploy
    on:
      workflow_run:
        workflows: ["CI - Verificação"]
        types: [completed]
    jobs:
      deploy:
        runs-on: ubuntu-latest
        if: ${{ github.event.workflow_run.conclusion == 'success' }}
        steps:
          - run: echo "Iniciando implantação automática..."
    ```
